import React from 'react';

export const Header = () => {

    return (
        <nav className="navbar navbar-expand-lg fixed-top navbar-dark bg-primary">
            <p className="navbar-brand">Hooks examples</p>
        </nav>
    );
};